# REXI
REXI focuses on designing and implementing a custom expression language from scratch. The language will support arithmetic expressions, variables, functions, and control flow constructs, and will be executed using an interactive command-line interpreter (CLI). The core of the system will be built using recursive descent parsing.
